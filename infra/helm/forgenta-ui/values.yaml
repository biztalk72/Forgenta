# forgenta-ui 차트 기본값 - 웹 대시보드
web:
  enabled: true
  image: forgenta/web:latest
  port: 80
  ingress:
    enabled: true
    className: traefik
    host: forgenta.localhost   # http://forgenta.localhost:8080
