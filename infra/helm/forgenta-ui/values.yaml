# forgenta-ui 차트 기본값 - 웹 대시보드
web:
  enabled: true
  image: forgenta/web:latest
  port: 80
  ingress:
    enabled: true
    className: traefik
    host: ""   # 비우면 catch-all → http://localhost:8080. 값 설정 시 해당 호스트로 라우팅
