{{- if .Values.priorityClass.create }}
apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: {{ .Values.priorityClass.name }}
value: {{ .Values.priorityClass.value }}
globalDefault: false
description: "Forgenta LLM 추론 파드 우선순위 — 자원 경합 시 evict 보호"
{{- end }}
